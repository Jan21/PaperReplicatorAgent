```json
{
    "section_title": "UNSAT-Core",
    "section_purpose": "To detail the implementation of a supervised baseline method that predicts UNSAT-core membership of variables for solver guidance, and to describe how it is adapted for a direct comparison with the paper's main RLAF approach.",
    "key_points": [
        "Implements a supervised GNN model to predict if a variable is part of an UNSAT-core, using a binary classification task with cross-entropy loss.",
        "Ground truth labels for training are derived from non-minimal UNSAT cores extracted from DRAT proofs generated by the CaDiCaL solver.",
        "The method is only trained on the 3COL and CRYPTO problem distributions, as UNSAT-cores in the 3SAT training set are not a meaningful target.",
        "Adapts the prior UNSAT-core guidance strategy by integrating predictions via multiplicative variable weights instead of resetting VSIDS scores, enabling fair comparison with RLAF policies across different solvers.",
        "The variable weight transformation uses a tuned parameter alpha to scale the model's probability predictions, with a +1 offset ensuring weights are at least 1.0."
    ],
    "technical_details": {
        "algorithms": [
            "Supervised training of a GNN for binary classification (UNSAT-core membership prediction).",
            "Method to transform model predictions into variable weights for integration into the branching heuristic: w(x) = 1 + α * p_core(x)."
        ],
        "formulas": [
            "Variable weight transformation: w(x) = 1 + α * p_core(x), where p_core(x) is the predicted probability of UNSAT-core membership and α ≥ 0 is a tuned scaling parameter."
        ],
        "architectures": [
            "Graph Neural Network (GNN) model used for the supervised prediction task (specific architecture details are presumably in A.1/A.2)."
        ],
        "hyperparameters": {
            "α (alpha)": "Tuned on validation set from the set: {10^-4, 10^-3, 10^-2, 10^-1, 10^0, 10^1, 10^2, 10^3, 10^4}. Tuned separately for Glucose and March solvers.",
            "Loss function": "Standard cross-entropy loss"
        },
        "datasets": [
            "Training data for UNSAT-core prediction: unsatisfiable instances from 3COL and CRYPTO distributions.",
            "Training/Validation ground truth: UNSAT cores extracted from DRAT proofs generated by CaDiCaL solver."
        ]
    },
    "dependencies": [
        "Understanding of UNSAT-cores (concept from prior work by Selsam and Bjørner (2019))",
        "Knowledge of GNNs for SAT representation (referenced from Section 2.2 / Appendix A.1/A.2)",
        "Understanding of the paper's own guidance policy based on multiplicative weights (likely from Section 2.1 or 2.3)",
        "Context on the problem distributions (3SAT, 3COL, CRYPTO) from Appendix B.1",
        "Understanding of the base solvers (Glucose, March), referenced in A.3"
    ],
    "reproducibility_notes": [
        "The specific GNN architecture used for the supervised model must be known (see A.1/A.2).",
        "Preprocessing steps for generating the ground truth labels: using CaDiCaL solver to produce DRAT proofs, then extracting UNSAT cores (not minimal).",
        "The exact training setup (e.g., number of epochs, optimizer) for the supervised GNN.",
        "The discrete set of values used to tune the α hyperparameter.",
        "The requirement to train separate models on each problem distribution (3COL, CRYPTO) and only on unsatisfiable instances.",
        "Policy for setting literal polarities when using UNSAT-core guidance (p(x) = 1)."
    ]
}
```